<h1>Let's Eat Some Burgers</h1>

<h2>On The Menu</h2>

<ul>
  {{#each burgers}}
    <li>
      {{burger_name}} <button class="delete-burger" data-id="{{id}}">DEVOUR!</button>
    </li>
  {{/each}}
</ul>

<h2>You Pig! Look How Much You Ate!</h2>

<ul>
  {{#each burgers}}
    {{#if this.devoured}}
      <li>
        {{this.burger_name}}
      </li>>
    {{/if}}
  {{/each}}
</ul>

<h2>Add a Burger to the Menu</h2>
<form class="create-form">

  <div class="form-group">
    <label for="ca">I want to eat a:</label>
    <input type="text" id="ca" name="name">
  </div>

  <button type="submit">Add a Burger</button>
</form>
